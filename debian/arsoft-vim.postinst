#!/bin/sh -e
PKG=arsoft-vim

if [ "$1" = configure ] ; then
	dpkg-divert --add --package ${PKG} --rename \
				--divert /etc/vim/vimrc.distrib /etc/vim/vimrc
	[ \! -e /etc/vim/vimrc -o -L /etc/vim/vimrc ] && ln -sf /usr/share/arsoft-base/vim/vimrc /etc/vim/vimrc
fi

#DEBHELPER#

exit 0
